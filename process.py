import numpy as np


def resize_seq(frames: [np.ndarray], new_width):
    (
        """''
    scales all dimensions proportionally to the new width
    """
        ""
    )

    for f in frames:
        pass
